<div class="container">
  <h2 class="page-title search">-検索結果-</h2>
    <% @players.each do |player| %>
      <ul class="list-group">
        <li class="list-group-item">
          <div class="result-list">
            <%= attachment_image_tag player, :player_image, fallback: "no_image.jpg", class: "icon-search" %>
            <div class="">
              <strong class="result-name"><%= link_to player.name, player_path(player.id) %></strong>
            </div>
            <div class="class">
              <%= player.base %>/<%= player.grade %>
            </div>
            <div class="class">
              <%= player.school %>/<%= player.enrollment %>
            </div>
            <% if current_school.already_liked?(player) %>
              <%= link_to '❤', player_like_path(player), method: :delete %>
            <% else %>
              <%= link_to '🤍️', player_likes_path(player_id: player.id), method: :post %>
            <% end %>
          </div>
        </li>
      </ul>
    
    <% end %>
  </table>
</div>