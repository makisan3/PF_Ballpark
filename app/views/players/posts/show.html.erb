<div class="post-body">
  <h2><%= @post.player.name %>のNote</h2>
  <p>写真: <%= @post.player.player_image %></p>
  <p>選手名：<%= @post.player.name %></p>
  <p>投稿日時：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  <p>タイトル：<%= @post.title %></p>
  <p>本文：<%= @post.body %></p>
  
</div>